# Task Management CRUD App

Full stack application built with Angular + Spring Boot + MySQL.

## Project Structure
- `/frontend` — Angular 17 with Angular Material
- `/backend` — Spring Boot 3.2 REST API

## Run Backend
cd backend
mvn spring-boot:run

## Run Frontend
cd frontend
ng serve